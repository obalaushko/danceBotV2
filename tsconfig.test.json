{
    "extends": "./tsconfig.json", // Використовуйте ваш основний tsconfig.json або налаштуйте відповідно до потреб
    "compilerOptions": {
      "target": "ESNext",
      "module": "CommonJS", // Важливо використовувати CommonJS для тестів
      // Додаткові налаштування, які вам можуть знадобитися для тестів
    },
    "include": [
      "__tests__/**/*.ts", // Змініть шлях відповідно до вашої структури проекту
      "src/**/*.ts" // Змініть шлях відповідно до вашої структури проекту
    ],
    "exclude": ["node_modules"]
  }
  